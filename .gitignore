# dependencies
node_modules/

# builds
dist/
build/
.expo/
apps/admin/dist/

# secrets — never commit these
.env
.env.local
.env.*.local
*.pem
*.key

# runtime artefacts — uploaded KYC documents and delivery photos live here
uploads/
*.log

# editor / OS
.DS_Store
.idea/
.vscode/

# Ruflo local secrets and runtime data
.claude-flow/data/
.claude-flow/logs/
.claude-flow/sessions/

# agent runtime state — machine-local, and memory.db churns a 600KB binary diff
# on every run. schema.sql and config.yaml are checked in; the state is not.
.swarm/memory.db
.swarm/memory.db-shm
.swarm/memory.db-wal
.swarm/backups/
.claude-flow/daemon-state.json
.claude-flow/daemon.pid
.claude-flow/harness-active-policy.json
.claude-flow/metrics/
.claude-flow/policy/
